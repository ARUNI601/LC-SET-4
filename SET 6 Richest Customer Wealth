class Solution(object):
    def maximumWealth(self, accounts):
        a=[]
        for i in range(len(accounts)):
            b=sum(accounts[i])
            a.append(b)
        return max(a)
        
